<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gd.Final.Mapper.loginMapper">
	<select id="selectUserBylogin" resultType="String">
		SELECT u.username
		from User u 
		JOIN UserPwhistory up 
		ON u.user_id = up.user_id 
		WHERE u.user_id=#{userId} AND up.user_pw=#{userPw};
	</select>
	
	<select id="selectAdminBylogin" resultType="String">
		SELECT a.name
		from Admin a 
		JOIN AdminPwhistory ap 
		ON a.admin_id = ap.admin_id 
		WHERE a.admin_id=#{adminId} AND ap.admin_pw=#{adminPw};
	</select>
	
</mapper>